[phases.setup]
nixPkgs = ["chromium", "nss", "nspr", "atk", "cups", "dbus", "expat", "libdrm", "libxkbcommon", "mesa", "libgbm", "alsa-lib"]

[phases.install]
cmds = ["npm ci", "npx playwright install chromium"]

[start]
cmd = "node server.js"
